# JuniorTest
Написать проект WebAPI в котором будет реализован метод API поиска диалога с теми идентификаторами клиентов, которые были переданы в метод.
return result ?? Guid.Empty; - это оператор проверяющий на нулл. Если result нулл, возвращаем Guid.Empty, если не нулл, оставляем result
FirstOrDefault возвращается первый найденный элемент, если он не найдет, то вернет нулл
